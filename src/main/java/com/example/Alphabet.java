package com.example;
public class Alphabet {
 
    
    // Declarar Alphabet

    //(Solo usaremos 'a' y 'b')

    // en el proyecto guía se implementa en esta clase el comportamiento del oraculo de membresia
    // pero nosotros lo podemos haccer en la misma clase Oracle, para que no parezca raro
}
